import random

print(random.random()) # Produce un float entre [0,1)
print(random.randint(1,10)) # Produce un int entre [n0, n1]
random.seed(23) # Establece una semilla para el generador de aleatorios
